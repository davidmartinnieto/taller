---
- name: Connect to python script
  hosts: AOTLXPPTHA00002
  user: icm
  vars:
  tasks:
   ## PRUEBA PS ##
    - name: Execute a "{{proceso}}" script
      shell: |
                                . ~/.profile && export LANG=en_US.UTF-8 && /icm/python3-virtualenv/bin/python "{{proceso}}"Automatizacion_INC_PS.py"{{capi_code}}"
      args:
        executable: /usr/bin/python
        chdir: /icm/auto_ps

